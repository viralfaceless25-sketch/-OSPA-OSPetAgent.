# OSPA Codex Resume — 2026-08-01

## Exact repository state

- Repository: `/Volumes/ai-hub/OSPA`
- Branch: `feat/sub-b-local-brain`
- Code/test checkpoint before this handoff document: `162a6ea`
- Final branch HEAD: the commit containing this file; resolve with `git rev-parse HEAD` and compare with `origin/feat/sub-b-local-brain`. The exact final SHA is also in the pause report because a Git commit cannot contain its own hash.
- Tree: clean at handoff (`git status --short` produced no output).
- Remote divergence: none at handoff (`git log origin/feat/sub-b-local-brain..HEAD` produced no output after the final push).
- Clean test: 219/219 tests passed in 26 suites. The command runner rejected literal `rm -rf .build`; `swift package clean && make test` performed the equivalent package-scoped clean rebuild.

## Step 2 status: PAUSED mid-implementation

Step 2 is not complete and must not be reported as complete.

Implemented and pushed:

- Plan: `docs/superpowers/plans/2026-08-01-intent-router.md` (`330c8ff`).
- Strict platform boundary: `Sources/AvatarPlatform/LocalBrainService.swift` (`fd98e3f`).
- `LocalBrainIntentLane` has exactly `native_app`, `chat`, and `unsupported`.
- The router request receives no application inventory and exposes three zero-argument tools with `additionalProperties: false`; it accepts exactly one empty-argument allowlisted tool call.
- `native_app` is only a lane value. The router cannot name an app, author arguments, construct a plan, or execute anything.
- Offline chat is a separate tool-free local-model request. Its returned display text is trimmed, limited to 1...2,000 Unicode scalars, and rejects control/format characters and multiline text.
- Eight new `MLXBrainClientTests` cover routing/chat boundaries; focused client suite was 20/20 green.

Not implemented:

- `AvatarModel` does not inject `LocalBrainIntentRouting` or `LocalBrainChatService` yet.
- `previewCommand()` still sends every deterministic-parser miss directly to `startBrainProposal(for:)`.
- There is no `startBrainRouting(for:)`, route completion handler, or chat task orchestration.
- Weather requests therefore still reach the app-picker in the current green tree.
- The fixed unsupported copy is specified in the plan/tests but is not wired into the UI model.
- Cancellation tests for late router/chat results are drafted but not active in the green tree.

The model-wiring RED tests were preserved as `fd736d4` and reverted by `162a6ea` so this pause ends with a green branch. Their observed compiler blocker was:

> `error: extra argument 'brainIntentRouter' in call`

and, for chat cases:

> `error: extra arguments at positions #3, #4 in call`

Review status: no Step 2 code review has run. Open review findings: none, because review has not run. Do not interpret that as review-clean.

## Resume here

First restore the exact RED model-test checkpoint:

```bash
cd /Volumes/ai-hub/OSPA
git cherry-pick fd736d4
swift test --filter AvatarModelBrainTests
```

The focused test command must fail with the missing `AvatarModel` router/chat injection errors above. Then implement Task 2 from `docs/superpowers/plans/2026-08-01-intent-router.md`: inject the router/chat services, replace only the final unparsed fallback with routing, send `native_app` into the existing `startBrainProposal(for:)` unchanged, publish chat text without action state, and use fixed copy for `unsupported`. Preserve `BrainProposalValidator` as the sole native-app safety authority. Continue TDD through GREEN before committing.

After any code edit, rebuild the derived graph exactly with:

```bash
graphify update .    # rebuild the code graph, no LLM, free
```

Never run bare `graphify .`; do not commit `graphify-out/`.

## Decisions and uncertainty

- Chosen boundary: one `MLXBrainClient` type conforms to three separate protocols (`LocalBrainService`, `LocalBrainIntentRouting`, `LocalBrainChatService`). This preserves distinct method/payload contracts without duplicating the loopback transport.
- Router gets no inventory. This structurally prevents app choice at the classification layer.
- Chat response validation is display-safety only; it does not make model text factually authoritative.
- “No network” was interpreted as no web or external network. Step 2 still uses the already-approved loopback MLX server because the roadmap explicitly requires the same local model. This interpretation has not received independent review.
- The shared private `send(payload:)` refactor preserves existing proposal behavior and passed the clean full suite, but has not received independent review.

Deliberately skipped: AvatarModel wiring, final Step 2 review/fix rounds, `make app` for Step 2 completion, web access, external networking, router persistence, evaluator, cloud escalation, new action kinds, PR changes, and merge.

## Durable pointers

- Step 2 ledger: `.superpowers/sdd/2026-08-01-intent-router/progress.md`
- Step 2 plan: `docs/superpowers/plans/2026-08-01-intent-router.md`
- Remaining project sequence: `docs/superpowers/ROADMAP-codex.md`
