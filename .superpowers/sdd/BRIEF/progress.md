# SDD ledger — plan: BRIEF.md

- Resume baseline: `9212519`; existing safe commits are `56d2a74` (multiline chat behavior and core tests) and `9212519` (AvatarView panel split).
- User ruling (2026-08-03): remove the stale multiline rejection from `MLXBrainClientTests.swift` and complete only the privacy-safe `AvatarModel` split.
- Binding constraint: consent, pending-plan/reason binding, generation-token, authorization, and audit flows remain private in `AvatarModel.swift`.
- Root cause reproduced: `swift test --filter MLXBrainClientTests` ran 24 tests with one failure because `"multiple\nlines"` remains in the existing invalid-answer table after multiline answers became valid.
- Task 1: in progress — finish privacy-safe model split, remove only the stale test case, verify, review, commit, and push.
- Task 1 implementation: removed only the stale multiline table entry and retained the maximal privacy-safe split: `AvatarModel+BrainMessages`, `+ConfidenceGate`, `+Presentation`, `+ReadPage`, `+Safety`, and `+TaskSequences`. All consent, plan/reason binding, generation-token, authorization, and audit flows remain private in `AvatarModel.swift`.
- Fresh RED/GREEN record: before removal, `swift test --filter MLXBrainClientTests` ran 24 tests with exactly one failure at line 279 because the accepted multiline result was still expected to throw; after removing only that entry, 24 passed.
- Verification: focused MLX (24), BrainProposalValidator (40), AvatarModelBrain (34), and AvatarModelReadPage (11) suites passed; `make test` passed 307 tests in 34 suites; `make app` passed; `graphify update .` refreshed the ignored graph (1,948 nodes / 5,124 edges).
- Task 1 review: independent read-only bounded review APPROVED with no findings; it confirmed the exact private→internal list is presentation-only, private binding/generation/read/audit state remains core-local, and `readApprovedPage` contains none of the six forbidden action-route symbols. No reviewer build/test ran.
- Task 1: complete in `00a1873` (`refactor(model): split safe AvatarModel helpers`); not pushed, merged, or submitted for PR. Detailed work, access audit, privacy boundaries, TDD history, and self-review: `.superpowers/sdd/BRIEF/task-1-report.md`.
- Post-completion final review found one sanitizer-boundary defect: `BrainChatAnswer.sanitized(_:)` trimmed before validation, so boundary CR, U+2028, U+2029, and U+200B disappeared before their forbidden Unicode categories were inspected.
- Fix round: runtime measurement confirmed all four scalars belong to Foundation `.whitespacesAndNewlines`. New pure-policy and MLX-client integration regressions each failed four times by returning `"Safe"`; validation now scans the original scalar stream before trimming, while the existing safe trimming, 2,000-scalar post-trim limit, LF/TAB allowance, and two-LF cap remain unchanged.
- Fix verification: focused policy suite passed 41 tests; focused MLX client suite passed 25; `make test` passed 309 tests in 34 suites; `make app` built and signed; `graphify update .` refreshed 1,950 nodes / 5,132 edges; `readApprovedPage` still contains none of the six forbidden action-route symbols.
- Focused independent re-review of the sanitizer fix returned READY with no Critical, Important, or Minor issues. Full-branch review remains the final gate before commit and push.
- Full-branch review found no code or safety defects, but returned NOT READY for one Important report omission (the required AvatarView line-count/access audit) and one Minor diff-cleanliness issue (six extra EOF blank lines). Fix round added the complete 1,122-line-before / eight-file-after table, enumerated both `@FocusState` and all 16 view-helper widenings with rationale, and removed only the six trailing blank lines. Scoped re-review pending.
- Scoped re-review confirmed the table, all 18 widened members, and a clean aggregate diff, then found one Important wording overclaim: several presentation helpers install handlers that delegate to existing model preview/confirmation gates. Corrected the report to state the structural invariant precisely—view helpers own no authority and the split neither changed nor bypassed any model gate. Final wording re-review pending.
- Final wording re-review returned READY with no remaining Critical, Important, or Minor issues; aggregate `git diff --check f637edc` remained clean. Branch is ready for fresh final verification, commit, and push only—no merge or PR.
- Final exact-tree verification after all code/review fixes: `make test` passed 309 tests in 34 suites; `make app` completed the production build and replaced the app signature; latest `graphify update .` produced 1,955 nodes / 5,136 edges. No live MLX or real app action was exercised.
